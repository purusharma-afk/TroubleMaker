const { context, logEvent, child, json, outageResponse } = require('../lib/observability');
module.exports = async function handler(req, res) { try { if (await outageResponse(req, res)) return; const ctx = context(req); await logEvent(ctx, { service: 'support-web', message: 'support form accepted', statusCode: 202 }); await child(ctx, 'support-api', 'support ticket created', { statusCode: 201 }); return json(res, 201, { ok: true, correlationId: ctx.correlationId }); } catch (error) { return json(res, 503, { ok: false, error: 'database_not_ready' }); } };
